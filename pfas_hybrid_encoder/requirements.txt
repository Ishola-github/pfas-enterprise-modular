# PFAS hybrid encoder (vector backbone). Prefer a Conda env for RDKit on Windows;
# pip wheels exist for many platforms (package name: rdkit).
numpy>=1.24
pandas>=2.0
rdkit>=2023.3.1
pyarrow>=14.0
