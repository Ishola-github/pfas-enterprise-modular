# Frozen validation artifacts (v1)

Place **copies** of pipeline outputs here for the declared freeze (see `../reports/EVIDENCE_COPY_CHECKLIST.md`).

- Prefer **small** JSON + **hashes** in git; large CSVs often stay local or under `../datasets/`.
- Always pair files with a `runs/<run_id>/manifest.json` entry.
