# External blind — results

Store **outputs** from the frozen model applied to external data (no retraining):

- Predictions CSV (exported from app or batch step)
- Confusion matrix / metrics text or JSON (if ground truth exists for a labeled subset)
- Any applicability-domain rejection logs

The narrative summary for sponsors and QA lives in **`EXTERNAL_BLIND_RESULTS_v1.md`** in this folder.
